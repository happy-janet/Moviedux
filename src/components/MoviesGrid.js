import React, {useState, useEffect} from 'react'; //this enables us to acess all react functionalities that we may need
import '../styles.css';

export default function MoviesGrid(){

    const [movies, setMovies] = useState([]);
    return(
        <div></div>
    );
}